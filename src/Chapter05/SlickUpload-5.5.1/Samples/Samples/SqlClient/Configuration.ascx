<%@ Control Language="C#" %>
<h1>Configuration</h1>
<p>Configuration for the SQL sample involves creating the file database and table and configuring SlickUpload
   to point at that database. To do this, perform the following steps:</p>
<ol>
    <li>Create a new database, or select an existing database to use</li>
    <li>Open and run the SlickUploadFile.sql script in the SlickUpload distribution package root folder on the selected database</li>
    <li>Change the connection string in the uploadStreamProvider web.config section to point to the database selected above</li>
</ol>
